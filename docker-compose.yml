services:
    api:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '3000:3000'
        env_file:
            - .env.docker

        depends_on:
            - db

    db:
        image: postgres:16
        ports:
            - '5433:5432'
        env_file:
            - .env.docker
